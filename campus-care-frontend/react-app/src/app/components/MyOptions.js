import React from 'react';

const MyOptions = ({options}) => {
    return (
        <div>
            {options.map(option =>
                <option> lmao</option>
            )}
        </div>
    );
};

export default MyOptions;
